<h1>Bonjour !</h1>

<p>Pour modifier votre mot de passe, utiliser le lien suivant :</p>

<a href="{{ url('gp_reset_password', {token: resetToken.token}) }}">{{ url('gp_reset_password', {token: resetToken.token}) }}</a>

<p>Ce lien expire dans
	{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>A bientôt.</p>
